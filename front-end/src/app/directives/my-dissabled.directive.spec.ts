import { MyDissabledDirective } from './my-dissabled.directive';

describe('MyDissabledDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDissabledDirective();
    expect(directive).toBeTruthy();
  });
});
